import "./styles.css"

const Header: React.FC<{}> = props => {
    return (
        <header className="todo-header">
            <h3 className="todo-header-heading">TODO List</h3>
        </header>
    )
}

export default Header